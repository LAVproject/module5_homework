// Произвольный массив
const arr = ['Тоска', 'Ржавый', 'Семнадцать', 'Рассвет', 'Печь', 'Девять', 'Добрый', 'Возвращение домой', 'Один', 'Товарный вагон']

// Выводим в консоль длину массива
console.log('arr.length', arr.length)

// Выводим в консоль каждый элемент массива
arr.forEach((value) => {
    console.log('value', value);
});
